---
apiVersion: v1
kind: Service
metadata:
  name: mongodb 
spec:
  ports:
  - protocol: tcp 
    port: 27017
    targetPort: 27017
  clusterIP: None
  selector:
    app: mongodb 
    project: roboshop
    tier: db 